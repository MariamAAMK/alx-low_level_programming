#include <stdio.h>

/**
 * main - Entry point
 * Description: 'print alphabets in lowercase except q and e'
 * Return: Always 0
 */

int main(void)
{

	int a = 97;

	while (a <= 122)
	{
		if (n == 101 || n == 113)
		{
			n++;
			continue;
		}
		putchar(n);
		n++;
	}
	putchar('\n');

		return (0);
}
